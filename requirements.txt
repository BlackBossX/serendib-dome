pygame>=2.5.0
numpy>=1.24.0
# scikit-learn removed – predictor now uses pure numpy.polyfit (WASM-compatible)

# ── Web / Vercel build ────────────────────────
# pygbag compiles pygame → WebAssembly for static hosting
pygbag>=0.9.2

